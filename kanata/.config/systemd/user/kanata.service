[Unit]
Description=Kanata keyboard remapper
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/env kanata --cfg %h/.config/kanata/config.kbd
Restart=on-failure
RestartSec=1
Environment=KDBUS=1

[Install]
WantedBy=default.target

