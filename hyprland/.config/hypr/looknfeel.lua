-- Change the default Omarchy look'n'feel.

-- Nothing overridden here for now:
--
-- - gaps_out: quattro ships its own quick toggle for zero gaps
--   (omarchy-hyprland-window-gaps-toggle, see
--   default/hypr/toggles/window-no-gaps.lua) — use that instead of
--   hardcoding gaps_out here.
-- - rounding: sticking with quattro's default (0, square corners). The old
--   pre-quattro rounding = 8 was dropped — purely cosmetic, not missed.
